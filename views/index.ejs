<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Four in a Row</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.socket.io/4.7.2/socket.io.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/phaser@3.60.0/dist/phaser.min.js"></script>
    <style>
        .game-shadow {
            box-shadow: 0 8px 0 #2563eb, 0 15px 20px rgba(0, 0, 0, .35);
            transition: all .2s ease;
        }
        .game-shadow:active {
            box-shadow: 0 4px 0 #2563eb, 0 8px 6px rgba(0, 0, 0, .45);
            transform: translateY(4px);
        }
        .cartoon-bg {
            background: linear-gradient(135deg, #a5f3fc 0%, #0ea5e9 100%);
        }
        .board-3d {
            transform: perspective(1000px) rotateX(10deg);
            transform-style: preserve-3d;
        }
    </style>
</head>
<body class="cartoon-bg min-h-screen flex items-center justify-center p-4">
    <div class="container max-w-4xl mx-auto">
        <div id="menu" class="bg-white rounded-2xl p-8 shadow-2xl text-center transform transition-all duration-300 ease-in-out">
            <h1 class="text-4xl font-bold mb-8 text-blue-600 tracking-wider">
                <span class="block transform hover:scale-110 transition-transform duration-200">Four in a Row</span>
            </h1>
            <div class="space-y-6">
                <button id="createRoom" class="game-shadow w-64 bg-blue-500 text-white text-xl font-bold py-4 px-8 rounded-full hover:bg-blue-600 transform hover:-translate-y-1 transition-all duration-200">
                    Create Room
                </button>
                <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
                    <input type="text" id="roomInput" placeholder="Enter Room Code" 
                           class="w-64 px-6 py-4 text-lg border-2 border-blue-300 rounded-full focus:outline-none focus:border-blue-500 text-center transform transition-all duration-200 hover:shadow-lg">
                    <button id="joinRoom" class="game-shadow w-64 sm:w-auto bg-green-500 text-white text-xl font-bold py-4 px-8 rounded-full hover:bg-green-600 transform hover:-translate-y-1 transition-all duration-200">
                        Join Room
                    </button>
                </div>
            </div>
        </div>
        
        <div id="gameContainer" class="hidden">
            <div id="gameInfo" class="bg-white bg-opacity-90 rounded-t-2xl p-4 mb-2 flex justify-between items-center">
                <span id="playerTurn" class="text-2xl font-bold text-blue-600 animate-pulse"></span>
                <span id="roomCode" class="text-lg font-semibold text-gray-600 bg-blue-100 px-4 py-2 rounded-full"></span>
            </div>
            <div class="board-3d bg-blue-600 rounded-2xl p-4 shadow-2xl"></div>
        </div>
    </div>
    <script src="/js/game.js"></script>
</body>
</html> 